<div><h1>详情<h1></div>
<div class="well_head">
 <div calss="establish">
   <div class="public_form">
     <lable class="lable">标题:</lable>
     <span class="validate-name"><%=@programme.name%></span>   
   </div>

   <div class="public_form">
     <label class="label">内容:</label>
     <span class="validate-name"><%=@programme.content.html_safe %></span>
   </div>

   <div class="public_form">
     <label class="label">图片:</label>
     <span class="validate-name">
       <% @programme.avatars.each do |ta|%>
         <a class="pic" href="<%= ta.photo.url %>" ><%= image_tag(ta.photo.url, size: "100x80", title: '点击放大') %></a>
       <% end %>
     </span>
   </div> 

   <%= link_to '修改', edit_backend_programme_path(@programme, :back => request.fullpath), class: 'search_button_s'%>
   <%= link_to '返回', params[:back], class: 'search_button_s' %>
 </div>
</div>

